@model IEnumerable<DataModel.Library.UserModel>

@{
    ViewBag.Title = "Welcome";
}

<!--<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Login</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" integrity="sha512-TzKXtysgRejqNoRfWzYjBIwfqleVi66X0ca78VGzF3Njs7enx9IF43f88JIjAQtYX2cg4fD6MpOys7Zyw+3lhQ==" crossorigin="anonymous" referrerpolicy="no-referrer" />

    <!-- Additional CSS for custom styles -->-->
    <!--@*<style>
            body {
                background-image: url('../../Images/Interview-guide-featured.jpg');
                /* Adjust background properties as needed */
                background-size: auto;
                background-repeat: no-repeat;
            }

            h2 {
                text-align: center;
            }
        </style>*@
    <link href="~/Content/welcome.css" rel="stylesheet" />
</head>-->

@*<body>*@
    @using (Html.BeginForm())
    {

        @*<nav class="navbar navbar-expand-lg navbar-light bg-light">
                <a class="navbar-brand" href="#">Interview Preparation</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item active">
                            <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#" id="profileBtn">
                                <i class="fas fa-user-circle"></i> Profile
                            </a>
                        </li>
                    </ul>

                    <ul class="navbar-nav ml-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("", "")">
                                <i class="fas fa-sign-out-alt"></i> Log Out
                            </a>
                        </li>
                    </ul>
                </div>
            </nav>*@

        @*<div class="container">
                <div>
                    <marquee><h3>Interview Preparation Reviews</h3></marquee>
                </div>

                <div class="row justify-content-center mt-5">
                    <div class="col-md-6 text-center">
                        <a href="@Url.Action("Index", "Review")">
                            <input type="button" name="name" class="btn btn-lg btn-primary" value="Go To Reviews" />
                        </a>
                    </div>
                </div>

            </div>*@

    }

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script>
          $(document).ready(function() {
            $('#profileBtn').click(function() {
                var userId = @Html.Raw(Json.Encode(Session["UserId"]));

                // Send AJAX request to retrieve user details using the user ID
                $.ajax({
                    url: '/User/UserDetails/' + userId,
                    method: 'GET',
                    success: function(data) {
                        // Assuming the data returned from the AJAX request contains the URL of the userdetails page
                        var userDetailsUrl = '/User/UserDetails/' + userId;

                        // Redirect the user to the userdetails page
                        window.location.href = userDetailsUrl;
                    },
                    error: function(xhr, status, error) {
                        // Handle error
                    }
                });
            });
        });
    </script>
@*</body>
</html>*@
